# hello-world MCP server — runtime dependencies
# -----------------------------------------------
# The server is implemented against the raw JSON-RPC 2.0 wire format
# using only the Python standard library, so there are NO third-party
# runtime dependencies.
#
# Optional: install the official MCP SDK if you want to extend the server
# using the high-level FastMCP API.
#   pip install "mcp[cli]>=1.0"
#
# Development / testing dependencies
pytest>=8.0
